/**
 * recipe-category router
 */

import { factories } from '@strapi/strapi';

// @ts-expect-error - Type will be generated after first build
export default factories.createCoreRouter('api::recipe-category.recipe-category');

