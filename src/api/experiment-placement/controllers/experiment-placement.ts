/**
 * experiment-placement controller
 */

import { factories } from '@strapi/strapi';

const uid = 'api::experiment-placement.experiment-placement' as any;

export default factories.createCoreController(uid);
