/**
 * experiment-variant controller
 */

import { factories } from '@strapi/strapi';

const uid = 'api::experiment-variant.experiment-variant' as any;

export default factories.createCoreController(uid);
