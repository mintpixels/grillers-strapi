{
  "kind": "collectionType",
  "collectionName": "back_in_stock_requests",
  "info": {
    "singularName": "back-in-stock-request",
    "pluralName": "back-in-stock-requests",
    "displayName": "Back in Stock Request",
    "description": "Customer requests to be notified when a specific OOS product is restocked (#102). Created from the storefront PDP capture form; processed by the restock trigger when inventory crosses zero."
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {
    "content-manager": {
      "visible": true
    },
    "content-type-builder": {
      "visible": true
    }
  },
  "attributes": {
    "Email": {
      "type": "email",
      "required": true,
      "regex": "^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$"
    },
    "MedusaProductId": {
      "type": "string",
      "required": true,
      "description": "Medusa `prod_…` id of the requested product."
    },
    "MedusaVariantId": {
      "type": "string",
      "required": false,
      "description": "Medusa `variant_…` id selected by the customer, when known. New inventory allocation flows should populate this."
    },
    "Sku": {
      "type": "string",
      "required": false,
      "description": "Customer-safe SKU subtext for the requested variant."
    },
    "QuickBooksListId": {
      "type": "string",
      "required": false,
      "description": "Stable QBD ListID/hex for operational matching. Do not expose in customer emails."
    },
    "ProductHandle": {
      "type": "string",
      "required": true,
      "description": "Storefront URL handle (used to build the restock email's product link)."
    },
    "ProductTitle": {
      "type": "string",
      "required": true,
      "description": "Snapshot of the product title at the time of request (so the restock email can show the name the customer originally saw)."
    },
    "RequestedFulfillmentDate": {
      "type": "date",
      "required": false,
      "description": "Optional requested date context if the waitlist was created from a date-specific allocation block."
    },
    "WaitlistReason": {
      "type": "enumeration",
      "enum": ["out_of_stock", "allocated_out", "future_unavailable"],
      "default": "out_of_stock",
      "required": false,
      "description": "Why the customer joined the notification list."
    },
    "NotifiedAt": {
      "type": "datetime",
      "required": false,
      "description": "Timestamp the restock email was sent. Null until the trigger fires."
    },
    "UnsubscribedAt": {
      "type": "datetime",
      "required": false,
      "description": "Set when the customer clicks the unsubscribe link in the confirm email — restock trigger skips them."
    },
    "UnsubscribeToken": {
      "type": "string",
      "required": true,
      "description": "Random token used to authenticate the unsubscribe link. This must remain filterable by the storefront server route; do not expose the back-in-stock API publicly."
    },
    "Source": {
      "type": "enumeration",
      "enum": ["pdp", "side_cart", "search"],
      "default": "pdp",
      "description": "Which storefront surface created the request — useful for funnel attribution."
    }
  }
}
